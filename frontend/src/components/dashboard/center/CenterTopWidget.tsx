import Card from "@/components/ui/Card";

const CenterTopWidget = () => {
  return (
    <Card className="flex h-full w-full flex-col rounded-xl border border-gray-900 bg-[var(--main-bg-color)]">
      {/*TODO: Content*/} Center Panel Top
    </Card>
  );
};

export default CenterTopWidget;
